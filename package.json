{
  "name": "photo-portfolio",
  "version": "1.0.0",
  "description": "Suhaib Khan landscape photography portfolio",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tools": "npm --prefix tools/importer install --silent && npm --prefix tools/importer start"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
